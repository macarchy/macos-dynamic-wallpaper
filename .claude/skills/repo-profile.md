# Repo profile

> Config for the issue/PR lifecycle skills (`create-issue`, `implement-issue`, `merge-pr`).
> Generated by `profile-repo`; re-run with `--refresh` after the toolchain/labels/CI change.
> This file is committed — it travels with the repo.

## Identity
- **Repo:** macarchy/macos-dynamic-wallpaper
- **Default branch:** main
- **What it is:** time-of-day wallpapers following the real sun

## Commit identity
- **Author line:** `git -c user.email=phmatray@gmail.com -c user.name="Philippe Matray"`
- **Source:** git config (no CLAUDE.md commit rule in this repo); cross-checked against the recent log,
  whose only human author is the same identity.

## Build & test
- **Build:** none — python3
- **Full test:** `python3 -m pytest tests/ -q`
- **Single-suite filter (per-task, fast):** `python3 -m pytest tests/test_dynamic_wallpaper.py -q`
- **Format/lint apply:** none — no formatter in this repo
- **Format/lint verify (the gate):** none — CI has no format gate; `bash -n` is the syntax gate where one exists
- **Prerequisites / caveats:** none noted

## CI gates (the exact commands CI fails on — satisfy these locally before ready/merge)
- `python3 -m pytest tests/ -q`
- `bash -n install.sh`

## Integration style
- **Merge mode:** squash — the only mode enabled on the repo (`allow_squash_merge: true`, the other two false)
- **PR title convention:** Conventional Commits `<type>(scope): subject`. **The PR title, not the branch
  commits, decides the release bump** — release-please reads the squashed subject on `main`
  (`fix:` patch, `feat:` minor, `feat!:`/`refactor!:` major). Squash appends `(#PR)`.
- **Branch naming:** `<type>/<slug>` (e.g. `feat/doctor-touchbar-modules`, `fix/ci-pytest`)

## Labels (apply verbatim; read live before use, this is a snapshot)
- **Type:** `bug`, `enhancement`, `documentation`
- **Priority tiers:** `priority: high` (pull before the other tiers) · `priority: medium` (the default tier) · `priority: low` (when the queue is otherwise empty)
- **Effort sizes:** `effort: small` (one task, one test cycle) · `effort: medium` (a handful of tasks inside one layer) · `effort: large` (cross-layer or phased) — **declaration order is the tier order**, smallest first
- **Scope/area:** `area: scheduler` (the solar clock: sun position to time-of-day phase) · `area: config` (the wallpaper-set config, its schema and its seeding) · `area: install` (install.sh, the systemd timer and the theme hook)
- **Source of truth:** `.github/repo-setup.yml` — `setup-repo`'s manifest. Edit it, then
  `repo-setup.sh apply`; never `gh label create` by hand.

## Issue templates
- **Location:** `.github/ISSUE_TEMPLATE/`
- **Forms:** feature_request.yml, bug_report.yml (from the kit)
- **Default for ideas:** feature_request · **for defects:** bug_report

## Tracker
- **Tracker:** github (github.com) — the lifecycle skills drive GitHub semantics through `gh`; any
  other value is refused at preconditions.

## Domain language
- none — no CONTEXT.md; fall back to house terminology and the README

## ADRs
- **Root:** none — no `docs/adr/`; `create-issue`'s brainstorm has nothing to check against here.
- **Server:** via AdrMcp (as `detect` measured on this machine — a session fact, not a repo fact).

## Out-of-scope records
- none — no rejected-ADR store and no `docs/out-of-scope/`; `create-issue` and `triage-backlog`
  report the prior-rejection lookup as unavailable rather than clean.

## Coding standards
- none stated — the review pass falls back to the Fowler smell baseline

## Conflict hot-spots (merge-with-main resolutions)
| File | Why it collides | Resolution |
|------|-----------------|------------|
| `CHANGELOG.md` | release-please appends; every branch carries its own entry | **regenerate** — let release-please rewrite it; never hand-merge |
| `.release-please-manifest.json` | every release bumps the one version | take the **higher** |
| `README.md`, `docs/` | parallel features document themselves | **union** |
| `tests/` | both sides append cases | **union** |
| `examples/dynamic-wallpaper.json` | the seeded default | **union**, then verify every named file is installed |
- **Rule of thumb:** union additive files, regenerate derived files, take-the-higher for the version;
  same-logic-both-sides is a real semantic conflict — understand both intents or stop.

## Architecture grain / invariants (shape implementation plans to these)
- The seeded default must name wallpapers a clean install actually has — see macarchy-install#9.
- install.sh only seeds a config when there is none; it never overwrites the user's choice.

## Worktree home
- `.claude/worktrees/` (verified ignored — the rule was added on 2026-09-05 and `worktrees-ignored.sh`
  exits 0; before it, `implement-issue` and `merge-pr` refused to create a worktree here)

## Environment gotchas
- This machine is an **aarch64 Asahi Linux M2 MacBook**, not a stock x86 box. Anything touching
  brightness, battery, the Touch Bar, trackpad gestures, themes or video is owned by a custom daemon.
- `git push` goes through the mise shim for its credential helper — never re-run `gh auth setup-git`.
- Two CPU clusters with a 1.58x gap (cores 0-3 = E, 4-7 = P): pin with `taskset` before timing anything.
